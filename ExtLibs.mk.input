#
# ExtLib.mk.input
# MegaMol
#
# Copyright (C) 2006 - 2008 by Universitaet Stuttgart (VIS).
# Alle Rechte vorbehalten.
#

# Paths to external libraries
outbin := %outbin%
vislibpath := %vislib%

ifeq (%withUserExpat%$1$0$, 1)
    expatpath := %expatPath%
    IncludeDir := $(IncludeDir) $(expatpath)/include
    LIBS := $(LIBS) expat$(BITS)
else
    LIBS := $(LIBS) expat
endif
