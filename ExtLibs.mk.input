#
# ExtLib.mk.input
# MegaMol
#
# Copyright (C) 2006 - 2008 by Universitaet Stuttgart (VIS).
# Alle Rechte vorbehalten.
#

# Paths to external libraries
outbin := %outbin%
vislibpath := %vislib%
visglutpath := %visglutPath%
mmcorepath := %mmcorePath%

ifeq (%withTweakbar%$1$0$, 1)
	tweakbarpath := %tweakbarpath%
	IncludeDir += $(tweakbarpath)/include
	CompilerFlags += -DWITH_TWEAKBAR
	LinkerFlags += -ltweakbarpath$(BITS)
	DebugLinkerFlags += -L$(tweakbarpath)/lib$(BITS)d 
	ReleaseLinkerFlags += -L$(tweakbarpath)/lib$(BITS)
endif
