
in vec4 objPos;
in vec4 camPos;
in vec4 outlightDir;
in float squarRad;
in float rad;
in vec4 vertColor;

// SPLAT
in float effectiveDiameter;

// OUTLINE / ifdef RETICLE
in float sphere_frag_radius;
in vec2 sphere_frag_center;
